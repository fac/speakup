<div class="well col-lg-12">
<%= form_for(@room, html: {class: "form-horizontal"}) do |f| %>
  <fieldset>
    <% if @room.errors.any? %>
    <div class="text-danger">
      <h4><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h4>

      <ul>
        <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name, class: "col-lg-1 control-label" %>
      <div class="col-lg-11">
        <%= f.text_field :name, class: "form-control" %>
      </div>
    </div>
    <div class="form-group">
      <div class="col-lg-10 col-lg-offset-1">
        <%= f.submit class: "btn btn-primary" %>
        <%= link_to 'Cancel', rooms_path, :class => 'btn btn-default' %>
      </div>
    </div>
  </fieldset>
<% end %>
</div>
